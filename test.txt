1Q.java -java(jdk) is the tool for executing byte code.
javac-javac(jdk)  is the tool for converting the source code to byte code.

2Q. JDK (java development kit) is Software development kit include tools for compiling and develping the java applications,
  JRE(java runtime environ ment) provides the required platform for excuting the java programs.
 JVM(java virtual machine) is responsible for  execte the  java byte code and memory allocation.

3Q.java-.java extension is used when we are saves the java program(its a Source code of the programe)
      .class-.class is a byte code(generated by Jvm)


4Q.java IDE's
eclips
JDK





5Q.variable: variable are used to store the information.
types of variales are
1.Static variables : static variable are declared inside the class,the static variables preserve the actual value even if its in out of the scope.
we can declare the static variabls inside the static Method because variables declared inside the mathods are act as Local variables.
Ex: class
{
static String name;?// static variables
static int age;
}

2.Local variables: local  variable are declare inside the method.
we can delclre local variable inside the parenthesis of the method,
Ex1: class  table                                                                                                                                                                              
{
}
void Method()
{
String name;// local variable
int number;
}

Ex2:
class life                                                                                                                                                                            
{
}
void Method(String name,int number)// local variable
)
{
 }

3.Instance variable: instance variables are declare inside the class but out side the Method.
Ex: class bus
{
String name;//instance variables
int number;
double cost;
Double speed;
}
void Bustop()
{
}

6Q. Object oriented programing in java
java is Object oriented programing language and platform indipendent,
OOPs concepts are:
1.Polymorphism: performing the same task(Methos) in differnt ways
Ex: Add();
      Add(int a,int b);
2. Abstration : its shows only required information and hiding the imlimentation part,
Ex: Mobile applications
 ATM machines,Tv.
3.Inheritance: Aquring the the propertis of Base class(Parent class) to derived calss(Child class),
Ex: animals
4.Encpsulation: Binding the variables and Method under the single Entity is called Encpsulation.
Ex: Class student
{
 String name;.//variables
int usn;
}
void read()//method
{
}
void write()//me
{
}
}

7Q.Main method Overloading
(its does not give any output)

8Q. Data types: datatype are used to store the attributes of variables.
types are:
premitive and non premitive data types
Premitive data types are: int ,short ,long,float,boolean,long,double,char.
Ex: int phno;
        double picode;

Non-Premitive datatypes are: String,Array[],Class(),interface.....any custom datatypes or developer creating datatypes.
Ex: int[] number={1,2,3,4,5}
      String name;


9Q. Dot operater is used for access variable followed by operator. 
Ex :  class Bangle
{
String type;
void Method()
{
System.out.println("type of bangle is:"+Bangle.type="metal");
}
}

10Q. java features are:
Platform indipendent: we can run the java program on any environment.
Object oriented : Abstration,polymorphism,inheritance,Encapsulation.
Security: in java Strong typed lines are there  for eg in case of array,we can not  initilization after declaration of array,if we declare after intilization its gives the error messare ArrayIndexOutOfBoundException.
simple: there is no concepts of Pointers,Structures,Operator Overloadng,Explicit memory allocation.
Robust: early checking of Error.
Portable: we can write java program in one platform and implemented in other platfom.
MultiThreading: cuncurrent execution of same part of programs at a time.
Distributed application: software that run on multiple computer connected to a network.
Architecureal nutral: Memory allocation can be done automatically in java.

11Q Array[]: array is used to grouping elements of same datatypes.
array is used to Store the elements of same datatype.
Collection of Homogenious elements.
Syntax: datatype[] variablename={};
Ex: int[] a={1,3,5,7,88};
Ex2. Class Student
{
String[] names={"kavya","raaju","ramya"};
}

12Q Class: Class is a bluePrint that object fallows.

Syntax: 
class Classname
{
//body of class
}
Ex: public class Xworks
{
public static void main(String[] args)
{
System.out.println(" message");
}
}

Object: real world entity, object is a Instance of class.
Ex: class Flower
{
String name;
}
void show()
{
}
public static void main(String[] args)
{
Flower flower(object name)=new flower();//object creation
show();
}
}

13Q.Key words:
Key word are Predefined in java,keywords hold the predifine values
Keywords are: Public,Private,protected
import,enum,if,do,else,final,super,static,null,new,this,while,switch,abstract,.

14Q.Constructer:
Constructer are used to initilize the instance variable,it do not have return type,constructer is used with new kayword,class name should be same has  constructer name.
Constructer is the special Metod.
constructer types are:
1.no-argument constructer
2.parameterized Constructer
3. default constructer

1. No-argument constructer: constructer with no arguments.
Ex: class Cap
{
String name;
Cap()// constructer with no argument(explicit constructer)
{
//body
}
}
2.Parameterized constructer:
constructer with parametes are called Parameterized constucter.
Ex:class School
{

School(String name,int usn)//explicit constructer
{
this.name=name;
this.usn=usn;
}
}
3. default constructer: default constructer provided by JVM.
class Pen
{
String brand;
}
-->implicit constructer type

15Q. 
Main method is static because main metod is the entry level of java program,the execution starts from the main metod ,the compiler search the main method for execution of the program.
main metod is static becuse main method is the entry level of the program.
Method types are:
Static metod 
Non static metod
1.Static method:
static method is the method we can access directly by specifying the name.
Static method declared with  in class.
Method name with the prefix word Static.
2.Non static method: it is a method it shuld have a return key word.
Ex: class Movie
{

int bookTicket(String MovieName,int cost)//non static method
{
return null;
}
}

16Q.
class Membres
{
int[] a={12,3,2,45,6,7,6}; 
public static void main(String[] args)
{
System.out.println(" length of array is:"+a.length);
}
}

17Q.Method:
method is a block of code , also called functions
Ex:calss Xworkz
{
}
void Btm()//Method declaration
{
//method body
}
Method Overloading: multiple metods having same name with different parametes are called Method overloading.
Ex:
class Perfume
{
String brand;
double cost;
String name;

Perfume()
{
}
Perfume(String brand,double cost)//Method overloading
{
}
Perfume(String name,Double cost,String name)//Method overloading
{
}
}
